[build]
  command = "npm run build"
  publish = "dist"

# SPA routing — toutes les URLs redirigées vers index.html (React Router gère le reste)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
